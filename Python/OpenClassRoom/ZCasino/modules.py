#coding:utf-8

def retry():

	while retryCheck == False:

		retryChoice = input("Retry ? (y/n)")
		retryChoice = retryChoice[1]

		for letter in retryChoice:

			if letter not in "yYnN":

				print("Y/N accepted only")
				continue

			elif letter in "yY":
			else:
				  
		